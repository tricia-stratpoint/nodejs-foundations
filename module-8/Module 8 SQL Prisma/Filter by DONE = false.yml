info:
  name: Filter by DONE = false
  type: http
  seq: 6

http:
  method: GET
  url: http://localhost:3000/tasks?done=false
  params:
    - name: done
      value: "false"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
