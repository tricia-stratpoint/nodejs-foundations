info:
  name: GET /tasks/:id
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:3000/tasks/1
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
