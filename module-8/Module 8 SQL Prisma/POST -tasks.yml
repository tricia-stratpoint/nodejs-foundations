info:
  name: POST /tasks
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:3000/tasks
  body:
    type: json
    data: |-
      {
        "title": "Learn Node.js",
        "tag": "training"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
