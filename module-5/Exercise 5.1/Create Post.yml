info:
  name: Create Post
  type: http
  seq: 5

http:
  method: POST
  url: https://jsonplaceholder.typicode.com/posts
  body:
    type: json
    data: |-
      {
        "title": "hello",
        "body": "world",
        "userId": 1
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
