info:
  name: Create User
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:3001/users
  body:
    type: json
    data: |-
      {
        "name": "Tricia",
        "email": "triciamonique.cuaresma@stratpoint.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
