info:
  name: False Tasks
  type: http
  seq: 9

http:
  method: GET
  url: http://localhost:3000/tasks?done=false
  params:
    - name: done
      value: "false"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
