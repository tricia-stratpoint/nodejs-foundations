info:
  name: Get all projects
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:3002/projects
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
