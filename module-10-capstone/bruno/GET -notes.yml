info:
  name: GET /notes
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:3000/notes
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
