info:
  name: GET /notes/:id
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost:3000/notes/1
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
