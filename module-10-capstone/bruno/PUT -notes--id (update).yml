info:
  name: PUT /notes/:id (update)
  type: http
  seq: 5

http:
  method: PUT
  url: http://localhost:3000/notes/5
  body:
    type: json
    data: |-
      {
        "title": "Shopping list",
        "content": "cheese, pizza",
        "tag": "personal"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
