info:
  name: Tasks with Title
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:3000/tasks
  body:
    type: json
    data: |-
      {
        "title": 123
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
